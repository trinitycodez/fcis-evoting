export interface LogoPropsType {
    layer: {
      layerText: boolean,
      layerImage: {
        border: boolean,
        height: number,
        width: number,
      },
      layerWrapper: boolean,
    }
}